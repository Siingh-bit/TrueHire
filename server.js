// Root entry point.
// Some hosts (including Hostinger's default) look for "server.js" at the project
// root to start the app. This simply launches the real Express + Socket.io server.
import './server/index.js';
